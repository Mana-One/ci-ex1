pipeline {
    agent {
        docker {
            image "node:17-alpine3.12"
        }
    }
    stages {
        stage("Build") {
            steps {
                sh "npm install"
            }
        }
        stage("Test") {
            steps {
                sh "npm test"
            }
        }
    }
}